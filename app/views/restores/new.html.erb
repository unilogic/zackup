<% content_for 'sub_menu' do %>
	<ul id="sub_menu">
			<li><%= link_to 'View Host', host_path(@host.id) %></li>
	</ul>
<% end %>

<% form_for [@host, @restore] do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>
<% end %>